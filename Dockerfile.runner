FROM n8nio/runners:latest

USER root

# 安装第三方包到官方 venv（runner 用的就是这个 venv 里的 Python）
RUN /opt/runners/task-runner-python/.venv/bin/pip install \
    httpx requests beautifulsoup4 feedparser \
    openai anthropic Pillow numpy pandas python-dotenv

USER runner
